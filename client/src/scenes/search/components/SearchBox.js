// lib imports
import React from 'react'
import styled from 'styled-components'

const SearchBox = ({ onChangeText }) => (
  <TextBox onChange={onChangeText} />
)

const TextBox = styled.input``

export default SearchBox
